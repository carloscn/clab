
fn test_move() {
    // wrong case:
    /*
    let s = vec!["duon".to_string(), "ramen".to_string(), "soba".to_string()];
    let t = s;
    let u = t;
    println!("{}", s[1]);   // the s and t memory move to the u;
    */
}

fn main() {
    test_move();
    println!("Hello, world!");
}
